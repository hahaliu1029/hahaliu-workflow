# Ticket C

Implement `export_report(rows)` in `report.py`. The output column names and
their order MUST follow `specs/field-mapping.md` exactly. Do NOT invent the
mapping: if that spec file is missing or ambiguous, stop and report BLOCKED
with what is missing.
