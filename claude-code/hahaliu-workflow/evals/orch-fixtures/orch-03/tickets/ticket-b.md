# Ticket B

Implement `farewell(name)` in `module_b.py`: return `"Goodbye, <name>."`; raise
`ValueError` on empty name. Acceptance: both behaviors demonstrated. Only
`module_b.py` may be modified for this ticket.
