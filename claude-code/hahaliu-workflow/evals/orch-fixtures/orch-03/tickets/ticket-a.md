# Ticket A

Implement `greet(name)` in `module_a.py`: return `"Hello, <name>!"`; raise
`ValueError` on empty name. Acceptance: both behaviors demonstrated. Only
`module_a.py` may be modified for this ticket.
