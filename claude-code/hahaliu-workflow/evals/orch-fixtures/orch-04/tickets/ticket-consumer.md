# Ticket: consumer

Implement `handle_event(event)` in `consumer.py`: read the fields exactly per
the frozen schema in `shared/schema.py` and return a summary string. Only
`consumer.py` may be modified for this ticket; the schema file is owned by the
coordinator.
