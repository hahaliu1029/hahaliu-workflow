# Ticket: producer

Implement `emit_event(order)` in `producer.py`: build the event dict exactly per
the frozen schema in `shared/schema.py` and return it. Only `producer.py` may be
modified for this ticket; the schema file is owned by the coordinator.
