"""Shared event schema.

EVENT SCHEMA: TBD — the event dict fields are NOT frozen yet. Freeze them here
(field names + types as a documented constant) before implementing producer or
consumer; both tickets must follow this file verbatim.
"""
