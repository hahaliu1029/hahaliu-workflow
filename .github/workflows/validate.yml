name: validate

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  release-validation:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate repository and both adapters
        run: scripts/validate-release.sh
